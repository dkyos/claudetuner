// Centralized configuration for Claude Tuner extension.
// Background service worker uses bg/constants.js (ES module) instead.
// Keep values in sync with bg/constants.js.
const CT_CONFIG = {
  DEFAULT_SERVER_URL: 'http://localhost:3000',
  DEFAULT_API_KEY: 'claude-manager-dev-key-2024',
  SITE_URL: 'http://localhost:3000'
};
